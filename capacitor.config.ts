import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zyntax.calculator',
  appName: 'Zyntex Calculator',
  webDir: 'dist'
};

export default config;
